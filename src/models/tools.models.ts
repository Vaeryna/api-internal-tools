export interface Tool {
    id: Number,
    name: String,
    description: Text,
    vendor: String,
    website_url: String,
    category_id: Number,
    monthly_cost: Number,
    active_users_count: Number,
    owner_department: Number,
    status: String
}